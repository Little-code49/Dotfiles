#  _                      _            _    
# | |__  _   _ _ __  _ __| | ___   ___| | __
# | '_ \| | | | '_ \| '__| |/ _ \ / __| |/ /
# | | | | |_| | |_) | |  | | (_) | (__|   < 
# |_| |_|\__, | .__/|_|  |_|\___/ \___|_|\_\
#        |___/|_|                           
# 

background {
    monitor =
    path = ~/.config/hypr/wallpapers/clouds-5.jpg
    blur_passes = 1
    blur_size = 2

}

input-field {
    monitor =
    size = 175, 40
    outline_thickness = 3
    dots_size = 0.33 # Scale of input-field height, 0.2 - 0.8
    dots_spacing = 0.15 # Scale of dots' absolute size, 0.0 - 1.0
    dots_center = true
    dots_rounding = -1 # -1 default circle, -2 follow input-field rounding
    outer_color = rgb(89dceb)
    inner_color = rgba(245, 194, 231, 0.2)
    font_color = rgb(11111b)
    fade_on_empty = true
    fade_timeout = 1000 # Milliseconds before fade_on_empty is triggered.
    placeholder_text = <i>Input Password...</i> # Text rendered in the input box when it's empty.
    hide_input = false
    rounding = -1 # -1 means complete rounding (circle/oval)
    check_color = rgb(204, 136, 34)
    fail_color = rgb(204, 34, 34) # if authentication failed, changes outer_color and fail message color
    fail_text = <i>$FAIL <b>($ATTEMPTS)</b></i> # can be set to empty
    capslock_color = -1
    numlock_color = -1
    bothlock_color = -1 # when both locks are active. -1 means don't change outer color (same for above)
    invert_numlock = false # change color if numlock is off
    swap_font_color = false # see below
    position = 0, -160
    halign = center
}

label {
    monitor =
    #clock
    text = cmd[update:1000] echo $(date +'%M')
    color = rgba(200, 200, 200, 1.0)
    font_size = 100
    font_family = JetBrainsMono Bold
    position = 0, 60
    halign = center
    shadow_passes = 5
    shadow_size = 10
}

label {
    monitor =
    #clock
    text = cmd[update:1000] echo $(date +'%H')
    color = rgba(200, 200, 200, 1.0)
    font_size = 100
    font_family = Jetbrains Mono Bold
    position = 0, 170
    halign = center
    shadow_passes = 5
    shadow_size = 10
}

label {
    monitor = 
    text = cmd[update:60000] echo $(date +'%B %d, %Y')
    font_family = Jetbrains Mono 
    font_size = 30
    color = rgba(200, 200, 200, 1.0)
    position = 0, -60
    halign = center
}

label {
    monitor =
    text = Hi, PugoruTori
    color = rgba(200, 200, 200, 1.0)
    font_size = 20
    font_family = Jetbrains Mono 
    position = 0, -100
    halign = center
    shadow_passes = 5
    shadow_size = 10
}

label {
    monitor = 
    text = cmd[update:60000] echo $(exec ~/.config/scripts/weather_info --getdata)\ $(exec ~/.config/scripts/weather_info --stat)\ $(exec ~/.config/scripts/weather_info --temp)\ $(echo "$(exec ~/.config/scripts/weather_info --icon)" ‎  )
    font_family = Jetbrains Mono  
    font_size = 20
    position = 0, 40%
    halign = center
}
